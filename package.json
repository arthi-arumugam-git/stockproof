{
  "name": "stockproof",
  "version": "0.1.8",
  "private": true,
  "type": "module",
  "description": "Reorder suggestions, purchase orders and barcode labels for Shopify merchants, from CSV exports, entirely in the browser.",
  "scripts": {
    "test": "vitest run",
    "serve": "python -m http.server 8099 --directory site"
  },
  "devDependencies": {
    "vitest": "^3.0.0"
  }
}
