{
  "name": "stockproof",
  "version": "0.2.2",
  "private": true,
  "type": "module",
  "description": "Reorder suggestions, purchase orders, barcode labels, stock counts and receiving for Shopify and WooCommerce merchants, from CSV exports, entirely in the browser.",
  "scripts": {
    "test": "vitest run",
    "serve": "python -m http.server 8099 --directory site"
  },
  "devDependencies": {
    "vitest": "^3.0.0"
  }
}
